# MEM_008 大单方向判断铁律

## 核心规则

**绝对禁止**用 `changePct`（全天累计涨跌幅）判断大单方向。

必须用 `data.price > prevPrice` 对比当前价与上一秒价格：
- `price > prevPrice` → 向上扫货
- `price < prevPrice` → 向下砸盘
- `prevPrice == 0`（首轮）→ 激烈成交

`changePct > 0` 代表全天累计涨跌，不能反映当前这一秒的买卖方向。
